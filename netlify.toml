# Netlify Configuration for Next.js App
# This enables full Next.js support including API routes, SSR, ISR, and Scheduled Functions
# Updated for Next.js 16.1.6 with proxy (formerly middleware) support

[build]
  command = "npm run build"
  functions = "netlify/functions"

# Environment-specific build settings
[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--legacy-peer-deps"
  NETLIFY_NEXT_SKEW_PROTECTION = "true"

# Functions Configuration
[functions]
  node_bundler = "esbuild"
  external_node_modules = ["@supabase/supabase-js", "web-push"]
  included_files = ["netlify/functions/**"]

# Scheduled function for reminders (runs every 30 minutes)
# Schedule is defined in the function itself using @netlify/functions schedule()

# Use the official Netlify Next.js plugin for full support
[[plugins]]
  package = "@netlify/plugin-nextjs"

# Headers for security and caching
[[headers]]
  for = "/*"
  [headers.values]
    # Default Cache-Control for HTML pages: always revalidate with server.
    # Static assets (/_next/static/*, /fonts/*, /icons/*) override this below.
    # This prevents browsers from serving stale HTML that references old chunk hashes.

    # Explicitly disable Netlify CDN caching for ALL HTML pages by default.
    # Without this, @netlify/plugin-nextjs may apply durable CDN caching to static
    # Next.js pages, causing stale HTML (with old chunk hashes) to be served after deploys.
    # Specific static pages (faq, terms, etc.) override this below with short CDN cache.
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    X-XSS-Protection = "1; mode=block"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"

# Cache static assets
[[headers]]
  for = "/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/_next/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Netlify Functions headers
[[headers]]
  for = "/.netlify/functions/*"
  [headers.values]
    Cache-Control = "no-store"
    Access-Control-Allow-Origin = "*"
    Access-Control-Allow-Methods = "GET, POST, OPTIONS"
    Access-Control-Allow-Headers = "Content-Type, Authorization"

# Service Worker headers
[[headers]]
  for = "/sw.js"
  [headers.values]
    Content-Type = "application/javascript; charset=utf-8"
    Cache-Control = "no-cache, no-store, must-revalidate"
    Service-Worker-Allowed = "/"

[[headers]]
  for = "/manifest.json"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"
    Content-Type = "application/manifest+json"

# PWA icons with long cache
[[headers]]
  for = "/icons/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Fonts with long cache
[[headers]]
  for = "/fonts/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# API routes - no caching, CORS enabled
[[headers]]
  for = "/api/*"
  [headers.values]
    Cache-Control = "no-store, no-cache, must-revalidate"
    Access-Control-Allow-Origin = "*"
    Access-Control-Allow-Methods = "GET, POST, PUT, DELETE, OPTIONS"
    Access-Control-Allow-Headers = "Content-Type, Authorization, X-Dev-Token"

# Developer dashboard API - stricter cache control
[[headers]]
  for = "/api/dev/*"
  [headers.values]
    Cache-Control = "no-store, no-cache, must-revalidate, private"

# ──────────────────────────────────────────────────────
# CDN-ONLY caching - reduces Netlify function invocations
# Netlify-CDN-Cache-Control only affects CDN, NOT the browser.
# Browser still follows Cache-Control set by Next.js.
# This means repeat visits within the cache window are served
# from CDN (no function invocation) while the browser still
# gets fresh content on each visit.
# ──────────────────────────────────────────────────────

# Homepage - brief CDN cache, no durable (deploy must invalidate immediately)
[[headers]]
  for = "/"
  [headers.values]
    Netlify-CDN-Cache-Control = "public, max-age=60, stale-while-revalidate=300"

# Legal/static pages - short CDN cache, no durable (deploy must invalidate immediately)
[[headers]]
  for = "/faq"
  [headers.values]
    Netlify-CDN-Cache-Control = "public, max-age=300, stale-while-revalidate=600"

[[headers]]
  for = "/terms"
  [headers.values]
    Netlify-CDN-Cache-Control = "public, max-age=300, stale-while-revalidate=600"

[[headers]]
  for = "/privacy-policy"
  [headers.values]
    Netlify-CDN-Cache-Control = "public, max-age=300, stale-while-revalidate=600"

[[headers]]
  for = "/accessibility"
  [headers.values]
    Netlify-CDN-Cache-Control = "public, max-age=300, stale-while-revalidate=600"

# Products catalog - moderate CDN cache, no durable
[[headers]]
  for = "/products"
  [headers.values]
    Netlify-CDN-Cache-Control = "public, max-age=300, stale-while-revalidate=600"

[[headers]]
  for = "/products/*"
  [headers.values]
    Netlify-CDN-Cache-Control = "public, max-age=300, stale-while-revalidate=600"

# Login page - brief CDN cache, no durable
[[headers]]
  for = "/login"
  [headers.values]
    Netlify-CDN-Cache-Control = "public, max-age=60, stale-while-revalidate=300"

# Notifications page (customer) - no CDN cache (real-time data)
[[headers]]
  for = "/notifications"
  [headers.values]
    Netlify-CDN-Cache-Control = "public, max-age=0, must-revalidate"
